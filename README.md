# miniStudentReportSystem-

A simple Java OOP example demonstrating a class, a constructor, and an object with basic instance fields.

Overview

This project defines a Student class with:

Two fields: name (String) and grade (int)
A constructor that initializes both fields
A displayInfo() method that prints a formatted summary

The Main class creates a Student object, sets/reads its fields, and prints the result.
